# {MODULE_NAME} — Business Rules

> Each rule is defined here ONCE. Everywhere else (requirements, edge cases, other modules) references the ID only.
> Cross-cutting rules live in `specs/rules.md` as `CBR-*`. If no module rules yet: `n/a` + one line why.

## BR-{MOD}-01: {Title}

- **Rule**: {statement — precise enough to implement without guessing}
- **Trigger**: {when it applies}
- **Exceptions**: {when it does not apply — or "none"}
- **Example**: {concrete example with real numbers/names}
- **Decided in**: D-{MOD}-{NN} *(or: from source input)*
