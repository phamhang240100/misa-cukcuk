# Cross-cutting Business Rules

> ONLY rules that span multiple modules live here, as `CBR-NN`. Module-specific rules live in `modules/{slug}/rules.md` as `BR-{MOD}-NN`.
> Each rule is defined once; everywhere else references the ID.

## CBR-01: {Title}

- **Rule**: {clear, unambiguous statement — one rule per entry}
- **Trigger**: {condition that activates this rule}
- **Applies to modules**: {list of module slugs — 2+ by definition, else it belongs in that module's rules.md}
- **Exceptions**: {cases where the rule does not apply — or "none"}
- **Example**: {concrete example with real data}
- **Decided in**: {D-{MOD}-NN / from source input}
